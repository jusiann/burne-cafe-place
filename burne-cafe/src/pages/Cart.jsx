import CartSection from '../components/CartSection';
import Layout from '../components/Layout';

function Cart() {
    return (
        <Layout>
            <CartSection />
        </Layout>
    );
}

export default Cart;
