import CartSection from '../../components/cart/CartSection';
import Layout from '../../components/layout/Layout';

function Cart() {
    return (
        <Layout>
            <CartSection />
        </Layout>
    );
}

export default Cart;
