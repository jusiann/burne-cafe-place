import Layout from '../components/Layout';
import CartSection from '../components/CartSection';

function Cart() {
    return (
        <Layout>
            <CartSection />
        </Layout>
    );
}

export default Cart;
